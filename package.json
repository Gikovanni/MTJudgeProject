{
  "name": "mtjudge-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm --prefix apps/mobile run start",
    "build": "npm --prefix apps/mobile run build",
    "test": "npm --prefix apps/mobile run test",
    "test:ci": "npm --prefix apps/mobile run test:ci",
    "lint": "npm --prefix apps/mobile run lint",
    "rules:index": "npm --prefix apps/mobile run rules:index",
    "rules:validate": "npm --prefix apps/mobile run rules:validate"
  }
}
